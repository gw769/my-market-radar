# 功能与评分说明

## 机会评分

每个平台独立计算：需求信号 40%、进入门槛 35%、价格空间 25%。搜索结果会先按标题与关键词做保守的相关性过滤，明显偏离关键词的结果仍保留在原始快照中，但不进入机会评分。

缺失公开字段不会被当成 0，也不会把剩余权重重新放大到 100%。缺失证据会把对应维度向中性分收缩，并降低数据完整度。只有相关样本数、价格覆盖、需求证据、核心评分证据和总体完整度都达到最低门槛时，平台才有资格输出“建议尝试 / 谨慎观察 / 暂不建议”；否则明确显示“数据不足”。

所选平台都达到门槛后才生成总体机会分。若只选择一个平台，则该平台达到门槛即可生成总体结论；选择两个平台时，两边都必须达标。

价格空间不再简单地把“价格越分散”视为越好。极端价格离散往往意味着混入不同规格、套装或子品类，因此会受到惩罚并提示进一步拆分关键词。

## 快照与趋势

每次成功采集生成一组快照。单平台失败或需要验证时，会保留已完成平台的检查点。趋势数据按关键词、平台和采集时间保留公开价格、已售、评论和搜索排名。

## 浏览器与验证

本地启动会显式使用项目自己的 Google Chrome/Chromium，并使用独立目录 `backend/data/browser-profile` 和 CDP 调试端口，不再依赖系统默认浏览器。Windows、macOS 和 Linux 会分别查找 Chrome/Chromium；也可通过 `BROWSER_EXECUTABLE` 手工指定路径。

Docker 镜像内置 Chromium，可在无桌面环境使用 headless CDP 完成普通公开页面采集；但验证码/风控页仍需要可见桌面和人工操作，系统不会绕过验证码。

## 边界

本工具不提供曝光率、转化率、广告费、退货率、利润预测或推算月销量，也不提供备货计划。商品标识只用于内部去重，用户无需输入或理解任何平台商品编号。
