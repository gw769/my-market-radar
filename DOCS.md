# 功能与评分说明

## 机会评分

每个平台独立计算：需求信号 40%、进入门槛 35%、价格空间 25%。某个维度所需公开字段缺失时，该维度不填零，剩余维度按原权重重新归一化。数据完整度和样本量始终单独展示。

只有 Shopee 与 Lazada 各取得至少 10 条有效结果时才生成综合分。结论分为“建议尝试”“谨慎观察”“暂不建议”，规则引擎不依赖大模型。

## 快照与趋势

每次成功采集生成一组不可变快照。单平台失败或需要验证时，不删除之前成功的快照。趋势表按关键词、平台和日期聚合公开价格中位数、平均排名和样本量。

## 边界

本工具不提供曝光率、转化率、广告费、退货率、利润预测或推算月销量，也不提供备货计划。商品标识只用于内部去重，用户无需输入或理解任何平台商品编号。
